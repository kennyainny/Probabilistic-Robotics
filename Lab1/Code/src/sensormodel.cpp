//Must calculate P(z|x)
//Load in map then place robot in open spaces and measure the distance to the wall and store that value.
#include "sensormodel.hpp"
#include <iostream>
#include <stdio.h>
#include <opencv2/opencv.hpp>

using namespace cv;
using namespace std;

void sensor_model(map_type map){
	count << map.size() << endl;
}