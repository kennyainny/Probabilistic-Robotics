#include "particle.hpp"
#include <iostream>
#include <random>
#include "math.h"

using namespace std;

state_type particle_filter(state_type p_state, laser_type laser)
{	//each particle is a set of state (x, y, theta)
		

	return p_state;
}