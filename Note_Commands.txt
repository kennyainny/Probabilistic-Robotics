cmake .. -G "MinGW Makefiles"
mingw32-make
./bin/lab1.exe //windows
../bin/lab1 //linux

git init //your working folder

git config --global user.email "your_email@youremail.com"
or
git config --global user.name "Your Name Here"

git config --global credential.helper cache

git config --global credential.helper 'cache --timeout=36000'

git remote add origin https://github.com/numkang/Probabilistic-Robotics

git add . //add all files to your "LOCAL" git

git commit -m 'Comment on what you have done' //compile

git push original master //upload all files

git pull original master //download all files (do this if fail to push)


